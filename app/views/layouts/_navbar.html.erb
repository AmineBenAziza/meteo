<nav class="navbar navbar-expand-lg navbar-light " style="background-color: #e3f2fd;">
  <div class="collapse navbar-collapse" id="navbar-collapse">
    <div class="navbar-nav mr-auto">
      <span class="nav-item active">
        <%= link_to "Home", root_path, class:"action-show nav-link" %>
      </span>
      <% if user_signed_in? %>
        <span class="nav-item active">
          <%= link_to "profile", edit_user_registration_path, class:"action-show nav-link" %>
        </span>
        <span class="nav-item active">
          <%= link_to "my city weather", weather_my_weather_path, class:"action-show nav-link" %>
        </span>
        <span class="nav-item active auth-link">
          <%= link_to "Log out", destroy_user_session_path, data: { "turbo-method": :delete }, class:"btn btn-warning mx-1" %>
        </span>
      <% else %>
        <div class="auth-link">
          <span class="nav-item active">
            <%= link_to "Sign In", new_user_session_path, class:"btn btn-primary mx-1" %>
          </span>
          <span class="nav-item active">
            <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-primary mx-1" %>
          </span>
        </div>
      <% end %>
    </div>
  </div>
</nav>
